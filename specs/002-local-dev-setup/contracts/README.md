# Contracts: Local Dev Setup

This feature introduces no new API endpoints. Local development surfaces are
bootstrapped via commands, and existing API behavior remains unchanged.

If future work adds local-only endpoints or health checks, add an OpenAPI schema
here.
