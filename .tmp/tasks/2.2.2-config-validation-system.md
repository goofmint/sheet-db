# 2.2.2 設定管理UI - バリデーションシステム

## ステータス
- [ ] 完了 / [x] 未完了  
- 開始日: 2025-07-31
- 完了日: 未完了

## 概要
設定管理UIの設定値バリデーションシステムを実装する。各設定項目に対して適切な検証ルールを適用し、無効な設定値の保存を防ぐとともに、ユーザーにわかりやすいエラーメッセージを提供する。

## 機能要件

### バリデーション機能
- **目的**: 設定値の整合性と有効性を保証
- **機能**:
  - フィールド別バリデーションルール
  - リアルタイムバリデーション
  - 依存関係バリデーション
  - カスタムバリデーター対応

### エラー表示機能
- **目的**: ユーザビリティの向上
- **機能**:
  - インラインエラーメッセージ
  - 日本語エラーメッセージ
  - フィールドハイライト
  - エラーサマリー表示

## データベース駆動バリデーションシステム

### Configテーブルの拡張
```sql
-- validation カラムを追加（JSON形式でバリデーションルールを保存）
ALTER TABLE Config ADD COLUMN validation TEXT; -- JSON形式

-- system_config カラムを追加（システム設定の識別）
ALTER TABLE Config ADD COLUMN system_config BOOLEAN DEFAULT 0;
```

### バリデーションルールのJSON構造
```typescript
interface ValidationRule {
  required?: boolean;
  type?: 'string' | 'number' | 'boolean';
  minLength?: number;
  maxLength?: number;
  min?: number;
  max?: number;
  pattern?: string; // 正規表現パターン（文字列として保存）
  enum?: string[];
  errorMessage: string;
  default?: string | number | boolean;
}

// Configテーブルのvalidationカラムに保存される例
const googleClientIdValidation: ValidationRule = {
  required: true,
  pattern: "^[0-9]+-[a-zA-Z0-9]+\\.apps\\.googleusercontent\\.com$",
  minLength: 50,
  errorMessage: 'Google クライアントIDは有効な形式（*.apps.googleusercontent.com）である必要があります'
};
```

### 初期バリデーションルール設定
```sql
-- Google OAuth2設定
UPDATE Config SET validation = '{"required":true,"pattern":"^[0-9]+-[a-zA-Z0-9]+\\.apps\\.googleusercontent\\.com$","minLength":50,"errorMessage":"Google クライアントIDは有効な形式（*.apps.googleusercontent.com）である必要があります"}' 
WHERE key = 'google.client_id';

UPDATE Config SET validation = '{"required":true,"minLength":24,"pattern":"^[A-Za-z0-9_-]+$","errorMessage":"Google クライアントシークレットは24文字以上の英数字である必要があります"}' 
WHERE key = 'google.client_secret';

UPDATE Config SET validation = '{"required":false,"pattern":"^[a-zA-Z0-9-_]{44}$","errorMessage":"Google スプレッドシートIDは44文字の英数字である必要があります"}' 
WHERE key = 'google.sheetId';

-- Auth0設定
UPDATE Config SET validation = '{"required":true,"pattern":"^[a-zA-Z0-9.-]+\\.(auth0\\.com|eu\\.auth0\\.com|au\\.auth0\\.com)$","errorMessage":"Auth0ドメインは有効な形式（*.auth0.com）である必要があります"}' 
WHERE key = 'auth0.domain';

UPDATE Config SET validation = '{"required":true,"minLength":32,"pattern":"^[A-Za-z0-9]+$","errorMessage":"Auth0クライアントIDは32文字以上の英数字である必要があります"}' 
WHERE key = 'auth0.client_id';

UPDATE Config SET validation = '{"required":true,"minLength":64,"pattern":"^[A-Za-z0-9_-]+$","errorMessage":"Auth0クライアントシークレットは64文字以上の英数字である必要があります"}' 
WHERE key = 'auth0.client_secret';
```

### ConfigServiceの拡張
```typescript
// ConfigServiceにバリデーション関連メソッドを追加
interface ConfigWithValidation {
  key: string;
  value: string;
  type: ConfigType;
  description: string | null;
  validation: ValidationRule | null;
  system_config: boolean;
}

// ConfigServiceに追加するメソッド
class ConfigService {
  // バリデーションルール付きで設定を取得
  static async getWithValidation(key: string): Promise<ConfigWithValidation | null> {
    const config = await this.get(key);
    if (!config) return null;
    
    const validation = config.validation ? JSON.parse(config.validation) : null;
    return {
      ...config,
      validation,
      system_config: config.system_config || false
    };
  }

  // 全設定をバリデーションルール付きで取得
  static async getAllWithValidation(): Promise<Record<string, ConfigWithValidation>> {
    const configs = await this.getAll();
    const result: Record<string, ConfigWithValidation> = {};
    
    for (const [key, config] of Object.entries(configs)) {
      const validation = config.validation ? JSON.parse(config.validation) : null;
      result[key] = {
        ...config,
        validation,
        system_config: config.system_config || false
      };
    }
    
    return result;
  }

  // バリデーションルールを設定
  static async setValidation(key: string, validation: ValidationRule): Promise<void> {
    await this.update(key, {
      validation: JSON.stringify(validation)
    });
  }
}

## バリデーター実装

### データベース駆動バリデーター
```typescript
class ConfigValidator {
  private configService: typeof ConfigService;

  constructor(configService: typeof ConfigService) {
    this.configService = configService;
  }

  // データベースからバリデーションルールを動的に取得
  private async getValidationRules(): Promise<Record<string, ValidationRule>> {
    const configs = await this.configService.getAllWithValidation();
    const rules: Record<string, ValidationRule> = {};
    
    for (const [key, config] of Object.entries(configs)) {
      if (config.validation) {
        rules[key] = config.validation;
      }
    }
    
    return rules;
  }

  // 単一フィールドの検証（非同期版）
  async validateField(key: string, value: string, allConfig: Record<string, string>): Promise<ValidationResult> {
    const rules = await this.getValidationRules();
    const rule = rules[key];
    
    if (!rule) {
      return { valid: true };
    }

    // 必須チェック
    if (rule.required && (!value || value.trim() === '')) {
      return {
        valid: false,
        error: `${key}は必須項目です`
      };
    }

    // 値が空の場合、必須でなければ有効
    if (!value || value.trim() === '') {
      return { valid: true };
    }

    // 型チェック
    if (rule.type && !this.validateType(value, rule.type)) {
      return {
        valid: false,
        error: rule.errorMessage
      };
    }

    // 長さチェック
    if (rule.minLength && value.length < rule.minLength) {
      return {
        valid: false,
        error: rule.errorMessage
      };
    }

    if (rule.maxLength && value.length > rule.maxLength) {
      return {
        valid: false,
        error: rule.errorMessage
      };
    }

    // 数値範囲チェック
    if (rule.type === 'number') {
      const numValue = parseFloat(value);
      if (rule.min !== undefined && numValue < rule.min) {
        return {
          valid: false,
          error: rule.errorMessage
        };
      }
      if (rule.max !== undefined && numValue > rule.max) {
        return {
          valid: false,
          error: rule.errorMessage
        };
      }
    }

    // パターンマッチング（文字列として保存されているため RegExp に変換）
    if (rule.pattern) {
      const regex = new RegExp(rule.pattern);
      if (!regex.test(value)) {
        return {
          valid: false,
          error: rule.errorMessage
        };
      }
    }

    // 列挙値チェック
    if (rule.enum && !rule.enum.includes(value)) {
      return {
        valid: false,
        error: rule.errorMessage
      };
    }

    return { valid: true };
  }

  // 全設定の検証（非同期版）
  async validateAll(config: Record<string, string>): Promise<ValidationResults> {
    const rules = await this.getValidationRules();
    const results: ValidationResults = {
      valid: true,
      errors: {}
    };

    // 全ルールを確認して、設定値または欠落をチェック
    for (const ruleKey of Object.keys(rules)) {
      const value = config[ruleKey] || '';
      const result = await this.validateField(ruleKey, value, config);
      if (!result.valid) {
        results.valid = false;
        results.errors[ruleKey] = result.error!;
      }
    }

    // 追加設定（ルールに定義されていない）も検証
    for (const [key, value] of Object.entries(config)) {
      if (!rules[key]) {
        const result = await this.validateField(key, value, config);
        if (!result.valid) {
          results.valid = false;
          results.errors[key] = result.error!;
        }
      }
    }

    // 依存関係チェック
    const dependencyErrors = this.validateDependencies(config);
    if (Object.keys(dependencyErrors).length > 0) {
      results.valid = false;
      results.errors = { ...results.errors, ...dependencyErrors };
    }

    return results;
  }

  // 依存関係の検証
  private validateDependencies(config: Record<string, string>): Record<string, string> {
    const errors: Record<string, string> = {};

    // ストレージ設定の依存関係
    const storageType = config['storage.type'];
    if (storageType === 'r2') {
      const requiredR2Fields = [
        'storage.r2.bucket',
        'storage.r2.access_key_id',
        'storage.r2.secret_access_key',
        'storage.r2.endpoint'
      ];
      
      for (const field of requiredR2Fields) {
        if (!config[field] || config[field].trim() === '') {
          errors[field] = `R2ストレージを使用する場合、${field}は必須です`;
        }
      }
    } else if (storageType === 'gdrive') {
      if (!config['storage.gdrive.folder_id'] || config['storage.gdrive.folder_id'].trim() === '') {
        errors['storage.gdrive.folder_id'] = 'Google Driveストレージを使用する場合、フォルダIDは必須です';
      }
    }

    // Auth0とGoogle の相互依存関係
    const hasAuth0Config = config['auth0.domain'] && config['auth0.client_id'] && config['auth0.client_secret'];
    const hasGoogleConfig = config['google.client_id'] && config['google.client_secret'];

    if (!hasAuth0Config && !hasGoogleConfig) {
      errors['auth0.domain'] = 'Auth0またはGoogleの設定が必要です';
      errors['google.client_id'] = 'Auth0またはGoogleの設定が必要です';
    }

    return errors;
  }

  // 型チェックヘルパー
  private validateType(value: string, type: string): boolean {
    switch (type) {
      case 'boolean':
        return value === 'true' || value === 'false';
      case 'number':
        return !isNaN(parseFloat(value)) && isFinite(parseFloat(value));
      case 'string':
      default:
        return typeof value === 'string';
    }
  }

  // 必須チェックヘルパー
  private isRequired(rule: ValidationRule, config: Record<string, string>): boolean {
    if (typeof rule.required === 'function') {
      return rule.required(config);
    }
    return rule.required || false;
  }
}
```

### バリデーション結果型定義
```typescript
interface ValidationResult {
  valid: boolean;
  error?: string;
}

interface ValidationResults {
  valid: boolean;
  errors: Record<string, string>;
}
```

## フロントエンドバリデーション

### リアルタイムバリデーション
```javascript
class ConfigFormValidator {
  constructor(validationRules) {
    this.rules = validationRules;
    this.setupEventListeners();
  }

  // イベントリスナーの設定
  setupEventListeners() {
    const configInputs = document.querySelectorAll('.config-input');
    
    configInputs.forEach(input => {
      // リアルタイムバリデーション（入力時）
      input.addEventListener('input', (e) => {
        this.validateField(e.target);
      });
      
      // フォーカス時の処理
      input.addEventListener('focus', (e) => {
        this.clearFieldError(e.target);
      });
      
      // フォーカス外し時の検証
      input.addEventListener('blur', (e) => {
        this.validateField(e.target);
      });
    });
  }

  // 単一フィールドの検証
  validateField(input) {
    const key = input.name;
    const value = input.value;
    const allConfig = this.getAllConfigValues();

    // バリデーション実行
    const result = this.validateSingleField(key, value, allConfig);
    
    if (result.valid) {
      this.showFieldSuccess(input);
    } else {
      this.showFieldError(input, result.error);
    }

    return result.valid;
  }

  // 全フィールドの検証
  validateAllFields() {
    const allConfig = this.getAllConfigValues();
    let isValid = true;
    const errors = {};

    for (const [key, value] of Object.entries(allConfig)) {
      const result = this.validateSingleField(key, value, allConfig);
      if (!result.valid) {
        isValid = false;
        errors[key] = result.error;
        
        const input = document.querySelector(`input[name="${key}"]`);
        if (input) {
          this.showFieldError(input, result.error);
        }
      }
    }

    // エラーサマリーの表示
    if (!isValid) {
      this.showErrorSummary(errors);
    } else {
      this.hideErrorSummary();
    }

    return isValid;
  }

  // フィールドエラーの表示
  showFieldError(input, message) {
    input.classList.add('error');
    
    // 既存のエラーメッセージを削除
    const existingError = input.parentNode.querySelector('.error-message');
    if (existingError) {
      existingError.remove();
    }

    // エラーメッセージを追加
    const errorElement = document.createElement('div');
    errorElement.className = 'error-message';
    errorElement.textContent = message;
    input.parentNode.appendChild(errorElement);
  }

  // フィールドエラーのクリア
  clearFieldError(input) {
    input.classList.remove('error', 'success');
    const errorMessage = input.parentNode.querySelector('.error-message');
    if (errorMessage) {
      errorMessage.remove();
    }
  }

  // フィールド成功の表示
  showFieldSuccess(input) {
    input.classList.remove('error');
    input.classList.add('success');
    
    const errorMessage = input.parentNode.querySelector('.error-message');
    if (errorMessage) {
      errorMessage.remove();
    }
  }

  // エラーサマリーの表示
  showErrorSummary(errors) {
    const errorCount = Object.keys(errors).length;
    const summaryElement = document.getElementById('error-summary') || this.createErrorSummary();
    
    summaryElement.innerHTML = `
      <div class="error-header">
        <span class="icon">⚠️</span>
        <strong>${errorCount}件のエラーがあります</strong>
      </div>
      <ul class="error-list">
        ${Object.entries(errors).map(([key, message]) => 
          `<li><strong>${key}:</strong> ${message}</li>`
        ).join('')}
      </ul>
    `;
    
    summaryElement.style.display = 'block';
    summaryElement.scrollIntoView({ behavior: 'smooth' });
  }

  // エラーサマリーの非表示
  hideErrorSummary() {
    const summaryElement = document.getElementById('error-summary');
    if (summaryElement) {
      summaryElement.style.display = 'none';
    }
  }

  // エラーサマリー要素の作成
  createErrorSummary() {
    const summaryElement = document.createElement('div');
    summaryElement.id = 'error-summary';
    summaryElement.className = 'error-summary';
    
    const form = document.getElementById('config-form');
    form.insertBefore(summaryElement, form.firstChild);
    
    return summaryElement;
  }

  // 全設定値の取得
  getAllConfigValues() {
    const inputs = document.querySelectorAll('.config-input');
    const config = {};
    
    inputs.forEach(input => {
      config[input.name] = input.value;
    });
    
    return config;
  }

  // 単一フィールドバリデーション（フロントエンド版）
  validateSingleField(key, value, allConfig) {
    // サーバーサイドのバリデーションロジックと同様の実装
    // 簡略化された例
    const rule = this.rules[key];
    if (!rule) return { valid: true };

    if (rule.required && (!value || value.trim() === '')) {
      return { valid: false, error: `${key}は必須項目です` };
    }

    if (rule.pattern && value && !rule.pattern.test(value)) {
      return { valid: false, error: rule.errorMessage };
    }

    return { valid: true };
  }
}
```

## CSS スタイリング

### バリデーション用スタイル
```css
/* フィールド状態 */
.config-input {
  border: 2px solid #ddd;
  padding: 8px 12px;
  border-radius: 4px;
  transition: border-color 0.3s ease;
}

.config-input:focus {
  outline: none;
  border-color: #007cba;
  box-shadow: 0 0 0 2px rgba(0, 124, 186, 0.2);
}

.config-input.error {
  border-color: #dc3545;
  background-color: #fff5f5;
}

.config-input.success {
  border-color: #28a745;
  background-color: #f0fff4;
}

/* エラーメッセージ */
.error-message {
  color: #dc3545;
  font-size: 0.875rem;
  margin-top: 4px;
  display: flex;
  align-items: center;
}

.error-message::before {
  content: '⚠️';
  margin-right: 4px;
}

/* エラーサマリー */
.error-summary {
  background-color: #f8d7da;
  border: 1px solid #f5c6cb;
  border-radius: 4px;
  padding: 16px;
  margin-bottom: 20px;
  display: none;
}

.error-header {
  display: flex;
  align-items: center;
  margin-bottom: 12px;
  color: #721c24;
}

.error-header .icon {
  margin-right: 8px;
  font-size: 1.2em;
}

.error-list {
  margin: 0;
  padding-left: 20px;
  color: #721c24;
}

.error-list li {
  margin-bottom: 4px;
}
```

## 実装内容

### 作成ファイル
```
src/utils/
├── config-validator.ts      # バリデーションクラス
├── config-validation-rules.ts  # バリデーションルール定義
└── validation-types.ts      # 型定義

static/js/
└── config-form-validator.js  # フロントエンドバリデーション

static/css/
└── config-validation.css    # バリデーション用スタイル

test/utils/
├── config-validator.test.ts # バリデーターテスト
└── config-validation-rules.test.ts  # ルールテスト
```

### 統合ファイル
```typescript
// src/api/v1/config/update.ts での使用例
import { ConfigValidator } from '../../utils/config-validator';
import { validationRules } from '../../utils/config-validation-rules';

const validator = new ConfigValidator(validationRules);

export const configUpdateHandler = async (c: Context) => {
  const { updates } = await c.req.json();
  
  // バリデーション実行
  const configToValidate = updates.reduce((acc, update) => {
    acc[update.key] = update.value;
    return acc;
  }, {});
  
  const validationResult = validator.validateAll(configToValidate);
  
  if (!validationResult.valid) {
    return c.json({
      success: false,
      message: 'バリデーションエラー',
      errors: validationResult.errors
    }, 400);
  }
  
  // 設定更新処理
  // ...
};
```

## テスト要件

### バリデーターテスト
1. **正常系テスト**
   - 有効な設定値の検証成功
   - デフォルト値の適用

2. **異常系テスト**
   - 必須フィールドの空値エラー
   - パターンマッチング失敗
   - 型チェック失敗
   - 依存関係エラー

### フロントエンドテスト
1. **リアルタイムバリデーション**
   - 入力時の即座な検証
   - エラー表示・非表示

2. **フォーム送信時バリデーション**
   - 全フィールド検証
   - エラーサマリー表示

## 関連ファイル

### 作成ファイル
- `src/utils/config-validator.ts` - データベース駆動バリデーションクラス
- `static/js/config-form-validator.js` - フロントエンドバリデーション
- `migrations/003_config_validation.sql` - バリデーションカラム追加

### 修正ファイル
- `src/api/v1/config/update.ts` - バリデーション統合
- `src/api/v1/config/get.ts` - フロントエンド統合
- `src/services/config.ts` - バリデーション関連メソッド追加
- `src/db/schema.ts` - Configテーブル拡張

### 参照ファイル
- `src/services/config.ts` - ConfigService

## メモ

### 実装方針
1. **データベース駆動設計**
   - バリデーションルールをConfigテーブルで管理
   - 動的なバリデーションルール変更対応
   - ハードコードの排除

2. **段階的実装**
   - データベーススキーマ拡張 → バリデーター実装 → UI統合
   - サーバーサイド → フロントエンド

3. **パフォーマンス考慮**
   - バリデーションルールのキャッシュ
   - 必要時のみデータベースアクセス
   - 非同期処理の適切な実装

4. **拡張性・保守性**
   - 新しい設定項目の容易な追加
   - バリデーションルールの柔軟な変更
   - JSON形式による構造化データ管理