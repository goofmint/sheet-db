# Task 1.3.1: API ルートルーティングの作成

## 概要
`src/api/index.ts`を作成し、APIエンドポイントの基盤となるルートルーティングシステムを実装する。

## 目的
- APIエンドポイントの統一された管理
- RESTful APIの基盤構築
- ルーティングの階層化と組織化
- 将来的なAPI拡張への対応

## 実装要件

### 1. ファイル構造
```
src/api/
├── index.ts          # ルートルーティング（このタスクの対象）
├── health/
│   └── get.ts        # ヘルスチェックAPI（次タスク）
├── setup/
│   ├── get.ts        # セットアップ画面（次タスク）
│   └── post.ts       # セットアップ処理（次タスク）
└── playground/
    └── get.ts        # Playground画面（次タスク）
```

### 2. 技術仕様

#### ルーティング設計
- **Hono Router**を使用したRESTful API設計
- **階層化されたルート構造**で保守性を向上
- **統一されたエラーハンドリング**の適用
- **ミドルウェアの一元管理**

#### APIエンドポイント構造
```
/api/
├── /health          # システムヘルスチェック
├── /setup           # 初期セットアップ
├── /playground      # API テストインターフェース
└── /sheets/         # Google Sheets API（将来実装）
    ├── GET    /     # シート一覧
    ├── POST   /     # シート作成
    └── /{name}/     # 個別シート操作
        ├── GET      # シートデータ取得
        ├── PUT      # シート更新
        ├── DELETE   # シート削除
        └── /data/   # データCRUD操作
```

### 3. 実装内容

#### コア機能
1. **ルートハンドラーの作成**
   - Honoインスタンスの作成と設定
   - 基本ミドルウェアの適用
   - エラーハンドリングの統合

2. **APIバージョニング対応**
   - `/api/v1/` プレフィックスでのバージョン管理
   - 将来的なAPIバージョン管理への対応

3. **レスポンス標準化**
   - 統一されたJSON レスポンス形式
   - エラーコードの標準化
   - メタデータの一貫した構造

#### ミドルウェア統合
- **リクエストID生成**（将来実装）
- **ログ記録**（将来実装）  
- **CORS設定**
- **レート制限**（将来実装）

### 4. エラーハンドリング

#### 標準エラーレスポンス形式
```typescript
interface ErrorResponse {
  error: {
    code: string;
    message: string;
    details?: any;
  };
  timestamp: string;
  path: string;
  requestId: string;
}
```

#### HTTPステータスコード
- `200` - 成功
- `201` - 作成成功
- `400` - リクエストエラー
- `401` - 認証エラー
- `403` - 認可エラー
- `404` - リソース未発見
- `500` - サーバーエラー

### 5. セキュリティ考慮事項

#### 基本セキュリティ
- **入力検証**の統一実装
- **SQLインジェクション対策**
- **XSS攻撃対策**
- **CSRF対策**（将来実装）

#### レート制限
- IP単位でのリクエスト制限
- API キー単位での制限（将来実装）
- 段階的制限（警告→制限→ブロック）

### 6. パフォーマンス考慮事項

#### キャッシュ戦略
- **レスポンスキャッシュ**の実装準備
- **ETags**による条件付きリクエスト対応
- **圧縮**の有効化

#### 最適化
- **非同期処理**の活用
- **バッチ処理**への対応準備
- **ストリーミングレスポンス**への対応準備

### 7. テスト戦略

#### 単体テスト
- ルーティング動作の検証
- エラーハンドリングの検証
- ミドルウェア統合の検証

#### 統合テスト
- API エンドポイント間の連携テスト
- エラーケースの網羅的テスト
- パフォーマンステスト

### 8. ドキュメント化

#### API仕様書
- OpenAPI/Swagger形式での仕様書生成準備
- エンドポイント一覧の自動生成
- 使用例の提供

#### 開発者向けドキュメント
- ルーティング追加手順
- エラーハンドリング拡張方法
- テスト作成ガイドライン

## 次のステップ

このタスク完了後の実装順序：
1. `src/api/health/get.ts` - ヘルスチェックAPI
2. `src/api/setup/get.ts` - セットアップ画面
3. `src/api/setup/post.ts` - セットアップ処理  
4. `src/api/playground/get.ts` - Playground画面
5. HTMLテンプレートの作成

## 成功基準

- [ ] `src/api/index.ts`が正常に作成される
- [ ] 基本的なルーティング機能が動作する
- [ ] エラーハンドリングが適切に機能する
- [ ] 既存のテストが全て通る
- [ ] 新しいAPIルートの追加が容易になる
- [ ] 統一されたレスポンス形式が確立される