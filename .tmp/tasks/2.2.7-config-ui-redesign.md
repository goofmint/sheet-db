# 2.2.7 GET /api/v1/configsを利用したGET /config の実装

## ステータス
- [ ] 未完了
- 開始日: 2025-08-04
- 完了日: 未定

## 概要

既存のGET /configエンドポイントを改修し、GET /api/v1/configsAPIを活用した設定一覧表示機能を実装する。現在はConfigService.getAll()を使用しているが、APIを経由した統一された表示方式に変更する。

## 現在の実装状況

### 既存機能
- `src/config/get.ts`: 基本的な設定表示・編集画面
- `src/templates/config/form.tsx`: 設定フォームテンプレート
- パスワード認証による保護
- CSRF保護
- センシティブ情報のマスク表示

### 改修が必要な箇所
1. ConfigService.getAll()からGET /api/v1/configsAPIへの切り替え
2. APIレスポンス形式に合わせたテンプレート修正
3. ページネーション対応（将来的な拡張のため）

## 実装仕様

### 1. APIの活用方針

既存のGET /api/v1/configs APIを活用:
- `GET /api/v1/configs` - 設定一覧取得のみ使用
- デフォルトパラメータ: `?limit=100&sort=key&order=asc`

### 2. UI設計

#### 2.1 メイン画面構成（既存維持）
```
┌─────────────────────────────────────────┐
│ [← Back to Playground]    [Logout]      │
├─────────────────────────────────────────┤
│ Configuration Management                │
├─────────────────────────────────────────┤
│ │Key             │Value    │Description││
│ │app.name        │SheetDB  │App name   ││
│ │cache.ttl       │600      │Cache TTL  ││
│ │google.client_id│*****    │OAuth ID   ││
└─────────────────────────────────────────┘
```

### 3. 技術実装詳細

#### 3.1 バックエンド修正
- **GET /config**: `src/config/get.ts`の最小限修正
  - 設定データ取得部分を削除
  - 空の設定リストでテンプレートを表示

#### 3.2 テンプレート修正
```jsx
// src/templates/config/form.tsx
<div class="config-table">
  <table id="config-table">
    <thead>
      <tr>
        <th>Configuration Key</th>
        <th>Value</th>
        <th>Description</th>
      </tr>
    </thead>
    <tbody>
      <tr><td colspan="3">Loading...</td></tr>
    </tbody>
  </table>
</div>

<!-- テンプレート定義 -->
<template id="config-row-template">
  <tr>
    <td class="config-key"></td>
    <td class="config-value"></td>
    <td class="config-description"></td>
  </tr>
</template>

<template id="config-value-text-template">
  <span class="config-text-value"></span>
</template>

<template id="config-value-secret-template">
  <span class="config-secret-value">*****</span>
</template>

<template id="config-value-boolean-template">
  <input type="checkbox" class="config-boolean-value" disabled>
</template>
```

#### 3.3 クライアントサイド実装
```javascript
// JavaScript (src/statics/config/client.js)
async function loadConfigs() {
  try {
    const response = await fetch('/api/v1/configs?limit=100&sort=key&order=asc');

    if (!response.ok) {
      // Surface the HTTP error to the caller/UI
      throw new Error(`HTTP ${response.status}`);
    }

    // 204 No-Content -> empty list
    const apiResult =
      response.status === 204 ? { data: { configs: [] } } : await response.json();
    const configs = apiResult.data.configs;
    
    updateConfigTable(configs);
  } catch (error) {
    // Don't log full error details in production
    if (process.env.NODE_ENV !== 'production') {
      console.error('Config load error details:', error);
    }
    
    if (error.message === 'Invalid password') {
      showErrorMessage('Invalid password. Please try again.');
    } else {
      showErrorMessage('Failed to load configuration data');
    }
  }
}

function updateConfigTable(configs) {
  const tbody = document.querySelector('#config-table tbody');
  const rowTemplate = document.querySelector('#config-row-template');
  
  // 既存の行をクリア
  tbody.innerHTML = '';
  
  configs.forEach(config => {
    const row = rowTemplate.content.cloneNode(true);
    
    // キー設定
    row.querySelector('.config-key').textContent = config.key;
    
    // 値設定
    const valueCell = row.querySelector('.config-value');
    if (config.type === 'boolean') {
      const boolTemplate = document.querySelector('#config-value-boolean-template');
      const boolElement = boolTemplate.content.cloneNode(true);
      const checkbox = boolElement.querySelector('.config-boolean-value');
      checkbox.checked = config.value === 'true';
      valueCell.appendChild(boolElement);
    } else if (config.type === 'secret' || config.sensitive === true) {
      const secretTemplate = document.querySelector('#config-value-secret-template');
      valueCell.appendChild(secretTemplate.content.cloneNode(true));
    } else {
      const textTemplate = document.querySelector('#config-value-text-template');
      const textElement = textTemplate.content.cloneNode(true);
      textElement.querySelector('.config-text-value').textContent = config.value;
      valueCell.appendChild(textElement);
    }
    
    // 説明設定
    row.querySelector('.config-description').textContent = config.description || '';
    
    tbody.appendChild(row);
  });
}

function showErrorMessage(message) {
  const tbody = document.querySelector('#config-table tbody');
  tbody.innerHTML = `<tr><td colspan="3" class="error-message">${message}</td></tr>`;
}

// ページ読み込み時に実行
document.addEventListener('DOMContentLoaded', loadConfigs);
```

### 4. セキュリティ考慮事項

#### 4.1 認証・認可
- 既存のパスワード認証を維持
- CSRF保護の継続

#### 4.2 エラーハンドリング
- API通信エラーの適切な表示
- フォールバック表示（API失敗時）

### 5. 実装ファイル構成

```
src/
├── config/
│   └── get.ts (最小限修正)
├── templates/config/
│   └── form.tsx (テーブル部分のみ修正)
└── statics/config/
    └── client.js (JavaScript追加)
```

## 依存関係

### 前提条件
- GET /api/v1/configs APIの実装完了 ✅

### 後続タスク
- なし（一覧表示のみの実装）

## メモ

### 設計上の注意点
1. 既存の認証システムとの互換性維持
2. サーバーサイド処理の最小化
3. クライアントサイドでのAPI呼び出し
4. エラー時のフォールバック表示