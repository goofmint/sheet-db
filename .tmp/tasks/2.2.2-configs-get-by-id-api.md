# [2.2.2] GET /api/v1/configs/:id API実装

## ステータス
- [ ] 未完了  
- 開始日: 未定
- 完了日: 未定

## 概要
特定の設定項目を取得するAPIを実装する。config_passwordによる認証後に指定したIDの設定項目詳細を取得できる機能を提供する。

## 実装内容

### APIエンドポイント
- **URL**: `GET /api/v1/configs/:id`
- **認証**: 必須（config_password認証）
- **レスポンス形式**: JSON

### パスパラメータ
- `id`: 設定項目のUUID

### レスポンス仕様
```typescript
interface ConfigResponse {
  id: string;
  key: string;
  value: string;
  description?: string;
  type: 'string' | 'number' | 'boolean' | 'json';
  isSystem: boolean;
  createdAt: string;
  updatedAt: string;
  metadata?: {
    validationRules?: object;
    displayOptions?: object;
  };
}
```

### エラーレスポンス
- 400: 不正なIDフォーマット
- 401: 認証エラー
- 403: 権限不足
- 404: 設定項目が見つからない
- 500: サーバーエラー

## 関連ファイル
- 作成: `src/api/v1/configs/[id]/get.ts`
- 参照: `src/repositories/config.ts`
- 参照: `src/middleware/auth.ts`
- 参照: `src/types/index.ts`

## 依存関係
- 前提: config_password認証ミドルウェア実装
- 前提: Config repositoryの存在
- 前提: 設定一覧取得API
- このタスクに依存: 設定更新API、設定削除API

## メモ
- UUIDバリデーション実装
- config_passwordによる認証チェック
- Configテーブルからの設定項目取得
- メタデータ情報の提供