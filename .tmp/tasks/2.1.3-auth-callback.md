# 2.1.3 Auth0コールバックAPIの作成

## ステータス
- [ ] 未完了
- 開始日: 2025-07-28
- 完了日: -

## 概要
Auth0からの認証コールバックを処理するAPIエンドポイント `src/api/v1/auth/callback/get.ts` を作成します。このエンドポイントは、Auth0での認証完了後にユーザーがリダイレクトされる先で、認証コードをアクセストークンに交換し、ユーザー情報を取得してJSONレスポンスを返します。

## 実装内容

### 1. コールバックAPIエンドポイント (`src/api/v1/auth/callback/get.ts`)

#### 主要機能
1. **認証コード検証**
   - クエリパラメータから認証コード（`code`）とstate取得
   - CSRFトークン（state）の検証
   - エラーレスポンスの処理

2. **トークン交換処理**
   - Auth0Serviceを使用してコードをアクセストークンに交換
   - IDトークンとアクセストークンの取得
   - トークンの検証

3. **ユーザー情報取得**
   - Auth0からユーザー情報を取得
   - 必要な情報の抽出（user_id、email、name等）

4. **認証情報のJSONレスポンス**
   - ユーザー情報
   - トークン情報（セキュアな形で）
   - 認証状態

#### API設計

**エンドポイント**: `GET /api/v1/auth/callback`

**クエリパラメータ**:
- `code` (required): Auth0からの認証コード
- `state` (required): CSRF保護用のstateパラメータ
- `error` (optional): エラー時のエラーコード
- `error_description` (optional): エラーの詳細説明

**レスポンス**:
- **成功時**: HTTP 200 with JSON
```json
{
  "success": true,
  "user": {
    "id": "auth0|xxxxxxxxxx",
    "email": "user@example.com",
    "name": "User Name",
    "picture": "https://..."
  },
  "tokens": {
    "access_token": "...",
    "id_token": "...",
    "expires_in": 86400
  },
  "authenticated": true
}
```

- **エラー時**: HTTP 400/500 with JSON
```json
{
  "success": false,
  "error": "authentication_failed",
  "message": "詳細なエラーメッセージ",
  "authenticated": false
}
```

#### 実装例
```typescript
import { Hono } from 'hono';
import { Auth0Service } from '../../../../services/auth0';
import { ConfigService } from '../../../../services/config';
import { Env } from '../../../../types/env';

const app = new Hono<{ Bindings: Env }>();

app.get('/', async (c) => {
  try {
    // クエリパラメータの取得
    const code = c.req.query('code');
    const state = c.req.query('state');
    const error = c.req.query('error');
    const errorDescription = c.req.query('error_description');

    // Auth0エラーの処理
    if (error) {
      return c.json({
        success: false,
        error: error,
        message: errorDescription || 'Authentication failed',
        authenticated: false
      }, 400);
    }

    // 必須パラメータの検証
    if (!code || !state) {
      return c.json({
        success: false,
        error: 'missing_parameters',
        message: 'Missing required parameters: code and state',
        authenticated: false
      }, 400);
    }

    // State検証（CSRF対策）
    const storedState = c.cookie('auth_state');
    if (!storedState || storedState !== state) {
      return c.json({
        success: false,
        error: 'invalid_state',
        message: 'Invalid state parameter',
        authenticated: false
      }, 400);
    }

    // リダイレクトURIの構築
    const currentHost = new URL(c.req.url).origin;
    const redirectUri = `${currentHost}/api/v1/auth/callback`;

    // Auth0トークン交換
    const auth0Service = new Auth0Service(c.env);
    const tokens = await auth0Service.exchangeCodeForToken(code, redirectUri);

    // ユーザー情報取得
    const userInfo = await auth0Service.getUserInfo(tokens.access_token);

    // Stateクッキー削除
    c.cookie('auth_state', '', { maxAge: -1 });

    // 成功レスポンス
    return c.json({
      success: true,
      user: {
        id: userInfo.sub,
        email: userInfo.email,
        name: userInfo.name,
        picture: userInfo.picture
      },
      tokens: {
        access_token: tokens.access_token,
        id_token: tokens.id_token,
        expires_in: tokens.expires_in
      },
      authenticated: true
    });

  } catch (error) {
    console.error('Callback error:', error);
    
    // エラーレスポンス
    return c.json({
      success: false,
      error: 'authentication_failed',
      message: 'Authentication process failed',
      authenticated: false
    }, 500);
  }
});

export default app;
```

### 2. ルーティング統合

#### APIルーターへの追加
`src/api/index.ts`に以下を追加：
```typescript
import callbackRouter from './v1/auth/callback/get';
v1.route('/auth/callback', callbackRouter);
```

### 3. セキュリティ実装

#### CSRF対策
- ログイン時に生成されたstateパラメータの検証
- 使用後のstateクッキー削除
- タイムアウト機能（stateの有効期限）

#### エラーハンドリング
- Auth0からのエラーレスポンスの適切な処理
- 無効なパラメータに対する400エラー
- サーバーエラーに対する500エラー
- セキュリティ情報の漏洩防止

#### トークンセキュリティ
- アクセストークンの適切な管理
- セキュアな情報の外部漏洩防止
- トークンの有効期限管理

## 関連ファイル
- 作成: `src/api/v1/auth/callback/get.ts`
- 更新: `src/api/index.ts` (ルーティング追加)
- 参照: `src/services/auth0.ts`
- 参照: `src/services/config.ts`

## 依存関係
- 前提となるタスク:
  - 2.1.1 Auth0サービス
  - 2.1.2 ログインAPI
  - 1.1.3 設定管理（ConfigService）
- このタスクに依存するタスク:
  - 2.1.4 ログアウトAPI
  - 2.1.5 現在のユーザー情報API
  - 2.2.1 セッション管理サービス

## セキュリティ考慮事項

### 1. CSRF攻撃対策
- **問題**: 悪意のあるサイトからの認証フローの誘導
- **対策**: stateパラメータによる検証とワンタイム使用

### 2. 認証コード漏洩対策
- **問題**: URLログやリファラーヘッダーでのコード漏洩
- **対策**: コードの即座な消費とトークン交換

### 3. セッション管理
- **問題**: トークンの永続化とセキュア管理
- **対策**: HTTPOnlyクッキーまたはセキュアセッション

## テスト戦略

### 単体テスト
- 正常な認証フローのテスト
- エラーケースの処理テスト
- State検証のテスト
- トークン交換の成功/失敗テスト

### 統合テスト
- Auth0サービスとの連携テスト
- 実際の認証フロー全体のテスト
- エラーハンドリングの統合テスト

### セキュリティテスト
- State改竄テスト
- 無効な認証コードテスト
- CSRF攻撃シミュレーション
- トークン検証テスト

## 未解決事項・TODO

### 1. セッション永続化方法
- **問題**: 取得したトークンの保存方法
- **選択肢**:
  - HTTPOnlyクッキー（XSS対策）
  - D1セッションテーブル
  - メモリキャッシュ（短期間）
- **推奨**: HTTPOnlyクッキー + D1セッション

### 2. トークンリフレッシュ機能
- **問題**: アクセストークンの期限切れ対応
- **実装**: refresh_tokenを使用した自動更新機能

### 3. ログイン後のリダイレクト先
- **問題**: 認証完了後のユーザー体験
- **選択肢**:
  - 固定ページ（/playground）
  - 元のページ（ログイン前の場所）
  - カスタムコールバック

## メモ
- Cloudflare Workers環境での適切なクッキー管理に注意
- Auth0の設定（Allowed Callback URLs）との整合性確保
- トークンの適切な有効期限管理
- **重要**: モック実装は絶対禁止 - 実際のAuth0サービスを使用すること
- レスポンス形式は後続のフロントエンド統合を考慮